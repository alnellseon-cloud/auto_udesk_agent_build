# auto_udesk_agent_build 使用手册

## 这个项目是做什么的

**用自然语言搭建和修改 UAgent 智能工作流。**

你可以让 Codex、Claude Code 等 AI 编排助手直接使用本项目，也可以手动调用 CLI。典型流程是：
1. 在 AI 编排工具中打开这个目录
2. 用中文描述你想要的工作流
3. AI 编排助手设计节点结构、调用平台 API 完成搭建

平台地址由 `.env` 中的 `UAGENT_BASE_URL` 配置，仓库不保存真实环境域名。

---

## 准备工作（只需做一次）

### 1. 安装依赖

```bash
cd /path/to/auto_udesk_agent_build
python3 -m venv .venv                       # 首次创建虚拟环境（只需一次）
.venv/bin/pip install -r requirements.txt
```

### 2. 配置登录凭据

打开 `.env` 文件（用 VS Code 或 Notepad++，保存时选 **UTF-8 无 BOM** 编码）：

```
UAGENT_BASE_URL=https://your-uagent-host.example.com
UAGENT_EMAIL=your@email.com
UAGENT_PASSWORD_MD5=你的密码的MD5值
UC_BASE_URL=https://your-user-center-host.example.com
```

**自动登录模式（推荐）**：配置邮箱、密码 MD5 和用户中心地址后，Token 过期时客户端会自动重新登录，无需手动操作。

> 密码哈希属于敏感凭据。请使用组织批准的方式生成和保管，不要把真实密码或哈希写入聊天、脚本或 Git。

**备用方式（手动 Token）**：如果不想配置密码，可以从浏览器复制 Token：
1. 打开当前环境的 UAgent 地址，按 F12 → Network
2. 任意点击操作，找到带 `Authorization` 请求头的请求
3. 复制完整的 `Bearer <token>` 值，填入 `.env` 的 `UAGENT_TOKEN=` 行
4. Token 约 4 小时过期，过期后需重复上述步骤

---

## 工作流类型

### 类型 A：纯对话工作流（无知识库）

适合：客服分流、问答、表单填写、数据提取等不需要查询文档的场景。

**操作步骤：** 直接描述需求 → AI 编排助手搭建 → 测试调优。

### 类型 B：带知识库的工作流

适合：产品手册查询、故障排查、代理商推荐等需要从文档中检索信息的场景。

**额外步骤：**
1. 准备原始文档（Word/Excel/PDF/PPT）放入 `tasks/<任务>/raw_material/`
2. AI 编排助手执行数据治理（格式规范化）→ 上传到 KMS → 同步到 UAgent 知识库
3. 验证切片质量，必要时手动调整
4. 搭建工作流并关联知识库节点
5. 测试调优

---

## 日常使用：开启新任务

### 第一步：打开项目

```bash
cd /path/to/auto_udesk_agent_build
```

可在 Codex、Claude Code 或 VS Code 中打开这个文件夹。

---

### 第二步：在 UAgent 平台新建应用（如需要）

1. 打开当前环境的平台页面，点击“新建应用”
2. 从浏览器地址栏复制 App ID（格式：`xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx`）

> 也可以让 AI 编排助手通过 API 创建应用。

---

### 第三步：新建任务目录

在 AI 编排助手里说：

```
帮我新建一个任务，工作流名叫"示例文本机器人"，App ID 是 <app_id>
```

AI 编排助手会自动创建：

```
tasks/example_text_bot/
  README.md          ← 变更日志（每次更新后追加记录）
  WORKFLOW.md        ← 工作流状态（AI 编排助手下次开会话必读）
  scripts/           ← 任务脚本
  logs/              ← 运行日志
  raw_material/      ← 原始文档（有知识库时）
  governed/          ← 治理后文档（有知识库时）
```

---

### 第四步：描述需求，让 AI 编排助手搭建

```
先读 tasks/example_text_bot/WORKFLOW.md，然后帮我搭建：
- 用户提问故障代码时，从知识库查找对应手册并回答
- 用户询问当地代理商时，从 Excel 数据库推荐就近总代
```

---

### 第五步：任务完成/调整后，更新两个文件

每次完成一个阶段，AI 编排助手会自动更新：

| 文件 | 内容 | 用途 |
|---|---|---|
| `WORKFLOW.md` | 节点结构、当前状态、已知问题 | AI 编排助手下次会话快速恢复上下文 |
| `README.md` | 变更历史：做了什么、测试结果 | 人类查看历史记录 |

**这两个文件都要更新**，否则下次开会话时信息会不完整。

---

### 第六步：测试工作流

AI 编排助手会在 `scripts/test_workflow.py` 中准备测试用例，覆盖各条路径。

你也可以直接说：
```
运行工作流测试，用这几个问题：
1. 我的机器报 E060 故障怎么排查？
2. 我在河南，想找最近的代理商
```

---

### 第七步：沉淀经验（可选）

完成编排后说：
> "请把这次编排的经验写入记忆"

AI 编排助手会按各自的记忆机制保存关键决策和踩坑记录；稳定经验应同步整理到 `docs/`。

---

## 常用操作示例

| 你想做什么 | 说什么 |
|---|---|
| 开始新任务 | "帮我新建一个任务，工作流名叫 XXX，App ID 是 YYY" |
| 继续已有任务 | "先读 tasks/xxx/WORKFLOW.md，然后帮我……" |
| 上传文档到知识库 | "把 raw_material/ 下的文档上传到指定知识库" |
| 查看切片效果 | "查看知识库中 xxx.docx 的切片内容" |
| 测试工作流 | "运行工作流测试，问题是：xxx" |
| 发布工作流 | "保存并发布" |
| 修改提示词 | "把 LLM 节点的提示词改为：……" |
| 查看所有应用 | "列出平台上所有的应用" |
| 调优回答格式 | "回答里的视频链接没有正确渲染，帮我修复提示词" |

---

## tasks/ 目录结构

```
tasks/
├── README.md              ← 所有任务总索引
├── _template/             ← 新任务模板（勿直接修改）
├── _shared/               ← 跨任务通用脚本
├── example_text_bot/      ← 示例：本地客户任务（不提交 Git）
│   ├── WORKFLOW.md        ← AI 编排助手必读：当前工作流状态
│   ├── README.md          ← 变更历史日志
│   ├── scripts/           ← 任务脚本
│   ├── logs/              ← 运行日志
│   ├── raw_material/      ← 原始文档
│   └── governed/          ← 治理后文档
└── your_new_task/         ← 你的新任务（按步骤创建）
```

---

## 常见问题

**Q：API 调用返回 401 怎么办？**
配置了邮箱、密码 MD5 和用户中心地址时，客户端会自动重新登录。如果使用手动 Token，需要重新获取并更新 `.env`。

**Q：保存工作流返回 400 conflict 怎么办？**
让 AI 编排助手重新读取当前工作流再保存（通常是 uuid 参数问题）。

**Q：工作流改坏了怎么恢复？**
UAgent 平台有版本历史，可在平台上回滚。或说"读取 scripts/build_workflow.py，重新部署一次"。

**Q：知识库检索结果不准确怎么办？**
可能是文档切片质量问题，让 AI 编排助手检查分片并提供优化建议（修改切片内容或重新治理文档）。

**Q：回复里的图片/视频链接显示为文字，没有渲染？**
这是 LLM 节点提示词问题，需要要求模型用平台支持的 HTML 标签输出。

**Q：日志文件中文是乱码？**
这是 Windows 编码问题，早期脚本可能有此问题。当前 `scripts/` 下的所有脚本已修复（开头有 UTF-8 编码设置）。旧日志文件中的乱码不影响功能。

**Q：AI 编排助手忘记了上次的进度？**
只要 `WORKFLOW.md` 保持最新，让助手先读取对应任务的 `WORKFLOW.md` 就能恢复上下文。这就是为什么每次变更后必须更新该文件。

---

## 项目文件总览

```
auto_udesk_agent_build/
├── 使用手册.md            ← 本文件（人类阅读）
├── AGENTS.md              ← Codex 自动读取的技术规范
├── CLAUDE.md              ← Claude Code 自动读取的技术规范
├── .env                   ← 登录凭据（不提交到 git）
├── .env.example           ← 配置示例
├── requirements.txt       ← Python 依赖
├── uagent/                ← UAgent 平台 API 封装层
│   ├── client.py          ← 自动登录客户端
│   ├── knowledge.py       ← 知识库操作（上传/同步/切片）
│   ├── workflow.py        ← 工作流 API（获取/保存/发布/测试）
│   ├── nodes.py           ← 节点工厂函数
│   ├── apps.py            ← 应用管理
│   └── models.py          ← 模型列表
├── builder/               ← 从 YAML 一键部署工具
├── tasks/                 ← 各工作流编排任务
└── examples/              ← 示例工作流 YAML
```
